# Implementation Plan: Production Payment Hardening

## Overview

Bottom-up hardening of the MIHAS Lenco payment integration for production readiness. Starts with database verification, then backend race condition and gate enforcement fixes, then frontend legacy cleanup and new admin UI, then wizard resilience, and finally property-based tests. Each phase ends with a checkpoint.

## Tasks

- [x] 1. Database schema verification and fixes via Neon
  - [x] 1.1 Verify `program_fees` partial unique index on `(program_id, fee_type, residency_category) WHERE is_active = true`
    - Run the verification SQL from the design document against Neon
    - If missing, create the index via SQL script in `backend/scripts/`
    - _Requirements: 13.1_
  - [x] 1.2 Drop conflicting full unique constraint `uq_program_fee_type_residency` from `program_fees`
    - This full UNIQUE constraint blocks soft-deleted records from being replaced with new active records
    - Create SQL migration: `ALTER TABLE program_fees DROP CONSTRAINT IF EXISTS uq_program_fee_type_residency;`
    - Execute via Neon MCP (prepare → verify → complete)
    - The partial unique index `uq_program_fee_active` remains and is sufficient
    - _Requirements: 18.1, 18.2, 18.3_
  - [x] 1.3 Add composite index `idx_payments_app_status` on `payments(application_id, status)`
    - Create SQL: `CREATE INDEX IF NOT EXISTS idx_payments_app_status ON payments (application_id, status);`
    - Execute via Neon MCP
    - _Requirements: 19.1, 19.2_
  - [x] 1.4 Seed initial program fees for all 4 programs
    - Insert active ProgramFee records for DRN, DCM, DEH, CPC with fee_type='application', residency_category='local', amount=153.00, currency='ZMW'
    - Use `INSERT ... ON CONFLICT DO NOTHING` for idempotency
    - Execute via Neon MCP
    - _Requirements: 20.1, 20.2_
  - [x] 1.5 Verify `webhook_event_logs` index on `reference` column
    - Run verification SQL against Neon
    - If missing, create the index via SQL script
    - _Requirements: 13.2_
  - [x] 1.6 Verify `payments` table has `lenco_reference`, `fee`, and `bearer` columns
    - Run verification SQL against Neon
    - _Requirements: 13.3_
  - [x] 1.7 Verify `applications.payment_status` column default is `pending`
    - Run verification SQL against Neon
    - _Requirements: 13.4_
  - [x] 1.8 Verify `application_documents` supports `nrc` and `passport` document_type values
    - Query existing document_type values or check constraint
    - _Requirements: 13.5_

- [x] 2. Checkpoint — Database verification complete
  - Ensure all indexes and columns are verified, ask the user if questions arise.

- [x] 3. Backend hardening — PaymentService race conditions and double-payment prevention
  - [x] 3.1 Add `SELECT FOR UPDATE` locking to `PaymentService._update_payment_status`
    - Wrap in `transaction.atomic()` with `Payment.objects.select_for_update().get(id=payment.id)`
    - Re-read payment status inside the lock before applying forward-only transition
    - If transition is not allowed, return as safe no-op
    - File: `backend/apps/documents/payment_service.py`
    - _Requirements: 7.1, 7.2, 7.3, 14.4_
  - [x] 3.2 Add `SELECT FOR UPDATE` to `process_webhook_event` payment lookup
    - Wrap payment lookup in `select_for_update()` inside `transaction.atomic()`
    - Ensures concurrent webhook events for the same payment are serialized
    - File: `backend/apps/documents/payment_service.py`
    - _Requirements: 7.3, 7.4_
  - [x] 3.3 Add double-payment initiation prevention to `initiate_payment`
    - Query for existing `pending` payment for the same `application_id` before creating
    - If found, return existing payment reference instead of creating a new record
    - File: `backend/apps/documents/payment_service.py`
    - _Requirements: 6.1, 6.2_
  - [x] 3.4 Add amount mismatch detection to `_update_payment_status`
    - Compare Lenco-reported amount with expected amount using `Decimal` comparison
    - Log warning and skip transition to `successful` if amounts differ
    - File: `backend/apps/documents/payment_service.py`
    - _Requirements: 17.1, 17.2, 17.3_

- [x] 4. Backend hardening — Submission gates in ApplicationReviewView
  - [x] 4.1 Add identity document check for submission
    - Query `ApplicationDocument.objects.filter(application_id=..., document_type__in=['nrc', 'passport'])`
    - Return `IDENTITY_DOCUMENT_REQUIRED` error if none found when `new_status == 'submitted'`
    - File: `backend/apps/applications/views.py`
    - _Requirements: 5.3, 5.4_
  - [x] 4.2 Add `SELECT FOR UPDATE` on application row for double-submission prevention
    - Wrap submission transition in `transaction.atomic()` with `select_for_update()` on the Application row
    - Verify application status is `draft` before transitioning to `submitted`
    - Return `ALREADY_SUBMITTED` error if status is not `draft`
    - File: `backend/apps/applications/views.py`
    - _Requirements: 8.1, 8.2, 8.4_
  - [x] 4.3 Add `PAYMENT_UNVERIFIED` check for approval without verified payment
    - When transitioning to `approved`, verify payment is verified or `force=true` is set
    - Return `PAYMENT_UNVERIFIED` error otherwise
    - File: `backend/apps/applications/views.py`
    - _Requirements: 4.6, 14.3_
  - [x] 4.4 Add backward compatibility for legacy `verified` payment status
    - Payment gate for submission: accept `payment_status = 'verified'` OR `successful` payment in `payments` table
    - Payment gate for approval: treat `verified` as equivalent to `paid`
    - Admin override: continue accepting `verified` as a valid status value
    - File: `backend/apps/applications/views.py`
    - _Requirements: 21.1, 21.2, 21.3_

- [x] 5. Checkpoint — Backend hardening complete
  - Ensure all backend changes compile and existing tests pass (`cd backend && python3 -m pytest`), ask the user if questions arise.

- [x] 6. Frontend legacy cleanup — Payment page rewrite
  - [x] 6.1 Rewrite `Payment.tsx` as read-only payment history
    - Remove ~800 lines of manual payment forms, `handleSubmitDeferredPayment`, `PaymentCompletionForm`, `paymentForms` state, `expandedApplicationId`, `submittingApplicationId`, all form inputs
    - Replace with: fetch payment records from `GET /api/v1/payments/?application_id=...`, display read-only list (status, amount, currency, timestamp), "Continue to Application Wizard" button
    - File: `apps/admissions/src/pages/student/Payment.tsx`
    - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_

- [x] 7. Frontend legacy cleanup — Types, schemas, hooks, and state machine
  - [x] 7.1 Remove `handleProofOfPaymentUpload` and `popFile` from `useWizardController`
    - Remove `handleProofOfPaymentUpload` callback, `handleProofOfPaymentUploadWrapped`, `popFile` from returned interface
    - Remove `baseHandleProofOfPaymentUpload`, `baseHandleProofOfPaymentFile` from `useApplicationFileUploads` destructuring
    - File: `apps/admissions/src/pages/student/applicationWizard/hooks/useWizardController.ts`
    - _Requirements: 2.1_
  - [x] 7.2 Remove `payment_method` enum from `applicationSchema.ts`
    - File: `apps/admissions/src/forms/applicationSchema.ts`
    - _Requirements: 2.3_
  - [x] 7.3 Remove `SET_PROOF_OF_PAYMENT` event and `hasProofOfPayment` from `applicationStateMachine.ts`
    - Update `canProceed()` for payment step to no longer check `hasProofOfPayment`
    - File: `apps/admissions/src/lib/applicationStateMachine.ts`
    - _Requirements: 2.4_
  - [x] 7.4 Remove deprecated payment fields from `useApplicationSubmit.ts`
    - Stop writing `payment_method`, `payer_name`, `payer_phone`, `amount`, `paid_at`, `momo_ref`, `pop_url` during submission
    - Submission should only set `status: 'submitted'` and `submitted_at`
    - File: `apps/admissions/src/hooks/useApplicationSubmit.ts`
    - _Requirements: 2.5_
  - [x] 7.5 Remove deprecated fields from `offline.ts` type definition
    - Remove `payer_name`, `payer_phone`, `momo_ref`, `pop_url` from `OfflineFormSubmissionData`
    - File: `apps/admissions/src/types/offline.ts`
    - _Requirements: 2.8_
  - [x] 7.6 Remove `pop_url` from Application type in `database.ts`
    - File: `apps/admissions/src/types/database.ts`
    - _Requirements: 2.2_
  - [x] 7.7 Update `usePaymentReceipt.ts` and `useDocumentGeneration.ts` to read from `payments` table
    - Replace `application.momo_ref`, `application.payment_method`, `application.paid_at`, `application.payment_verified_at` with data from `GET /api/v1/payments/?application_id=...`
    - _Requirements: 2.7_
  - [x] 7.8 Update admin components to replace `pop_url` and `momo_ref` references with `payments` table data
    - Update `ApplicationDetailModal`, `ApplicationCard`, `ApplicationsCards`, `DocumentsTab`, `ReportsGenerator`
    - _Requirements: 2.6_

- [x] 8. Checkpoint — Frontend legacy cleanup complete
  - Ensure admissions app builds without errors (`cd apps/admissions && bun run build`), ask the user if questions arise.

- [x] 9. Admin fee management UI
  - [x] 9.1 Create `ProgramFees.tsx` admin page
    - Table listing active program fees grouped by program (fee type, residency category, amount, currency)
    - Create/edit/delete fee forms calling `POST/PUT/DELETE /api/v1/programs/{id}/fees/`
    - Soft delete via `DELETE` (sets `is_active=false`)
    - Handle duplicate active fee validation error from backend
    - Accessible to admin and super_admin roles only
    - File: `apps/admissions/src/pages/admin/ProgramFees.tsx`
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_
  - [x] 9.2 Add admin payment status display and override support in review interface
    - Display current payment status and payment history from `payments` table in application review
    - Ensure payment status override via `POST /api/v1/applications/{id}/review/` records `admin_feedback` and `admin_feedback_by`
    - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5_
  - [x] 9.3 Register `ProgramFees` page in admin routing
    - Add route to the admin section of the router
    - Add navigation link in admin sidebar/menu
    - _Requirements: 3.1_

- [x] 10. Checkpoint — Admin UI complete
  - Ensure admissions app builds without errors, ask the user if questions arise.

- [x] 11. Application Wizard resilience improvements
  - [x] 11.1 Mark identity document upload as required in education step
    - Show validation error if no NRC or Passport document is uploaded when proceeding from education step
    - Update submit step readiness checklist to include identity document check
    - _Requirements: 5.1, 5.2, 5.5_
  - [x] 11.2 Add payment gate check before enabling submit step
    - Check payment status before enabling submit, display message directing student to complete payment
    - Disable pay button while initiation request is in flight; re-enable on Lenco `onClose` without success
    - _Requirements: 6.3, 6.4, 14.2_
  - [x] 11.3 Add auto-save pause during critical operations
    - Pause draft saving when on payment step with payment in progress (`initiating` or `pending`)
    - Pause draft saving during submission processing
    - _Requirements: 9.1, 9.2_
  - [x] 11.4 Add network resilience to wizard
    - Show "Payment widget unavailable" error when Lenco script fails to load with refresh suggestion
    - Show pending status message and continue polling on payment verification timeout
    - Show retry-capable error messages on network errors during wizard API calls
    - Preserve form data to localStorage on `beforeunload`
    - _Requirements: 10.1, 10.2, 10.3, 10.4_
  - [x] 11.5 Add session expiry handling during payment step
    - Preserve current draft to localStorage and redirect to sign-in with return URL
    - _Requirements: 10.5_
  - [x] 11.6 Add browser back/forward navigation support in wizard
    - Use `history.pushState` or React Router to navigate wizard steps on back/forward
    - _Requirements: 11.2, 11.3_
  - [x] 11.7 Disable submit button after first click until response received
    - Prevent double-click on submit button
    - _Requirements: 8.3_

- [x] 12. Checkpoint — Wizard resilience complete
  - Ensure admissions app builds without errors and existing tests pass, ask the user if questions arise.

- [x] 13. Backend property-based tests
  - [x] 13.1 Write property test for forward-only payment status transitions
    - **Property 1: Forward-only payment status transitions**
    - **Validates: Requirements 14.4, 7.3, 7.4**
    - File: `backend/tests/property/test_payment_transitions.py`
  - [x] 13.2 Write property test for double payment initiation returns existing reference
    - **Property 2: Double payment initiation returns existing reference**
    - **Validates: Requirements 6.1**
    - File: `backend/tests/property/test_payment_transitions.py`
  - [x] 13.3 Write property test for amount mismatch blocks status transition
    - **Property 3: Amount mismatch blocks status transition**
    - **Validates: Requirements 17.1, 17.2**
    - File: `backend/tests/property/test_payment_transitions.py`
  - [x] 13.4 Write property test for webhook signature computation correctness
    - **Property 4: Webhook signature computation correctness**
    - **Validates: Requirements 16.1**
    - File: `backend/tests/property/test_webhook_signature.py`
  - [x] 13.5 Write property test for invalid webhook signature rejection and logging
    - **Property 5: Invalid webhook signature rejection and logging**
    - **Validates: Requirements 16.4**
    - File: `backend/tests/property/test_webhook_signature.py`
  - [x] 13.6 Write property test for fee resolution correctness with fallback chain
    - **Property 6: Fee resolution correctness with fallback chain**
    - **Validates: Requirements 12.4, 15.1, 15.2, 15.3**
    - File: `backend/tests/property/test_fee_resolution.py`
  - [x] 13.7 Write property test for identity document required for submission
    - **Property 7: Identity document required for submission**
    - **Validates: Requirements 5.3, 5.4**
    - File: `backend/tests/property/test_submission_gates.py`
  - [x] 13.8 Write property test for payment gate enforcement on submission
    - **Property 8: Payment gate enforcement on submission**
    - **Validates: Requirements 14.1**
    - File: `backend/tests/property/test_submission_gates.py`
  - [x] 13.9 Write property test for submission requires draft status
    - **Property 9: Submission requires draft status**
    - **Validates: Requirements 8.1**
    - File: `backend/tests/property/test_submission_gates.py`
  - [x] 13.10 Write property test for admin payment status override records audit trail
    - **Property 10: Admin payment status override records audit trail**
    - **Validates: Requirements 4.2, 4.4, 4.5**
    - File: `backend/tests/property/test_admin_override.py`
  - [x] 13.11 Write property test for duplicate active program fee rejection
    - **Property 11: Duplicate active program fee rejection**
    - **Validates: Requirements 3.6**
    - File: `backend/tests/property/test_fee_resolution.py`
  - [x] 13.12 Write property test for webhook processing idempotence
    - **Property 12: Webhook processing idempotence**
    - **Validates: Requirements 7.4**
    - File: `backend/tests/property/test_webhook_signature.py`
  - [x] 13.13 Write property test for successful webhook syncs application payment_status
    - **Property 13: Successful webhook syncs application payment_status**
    - **Validates: Requirements 12.2**
    - File: `backend/tests/property/test_payment_transitions.py`
  - [x] 13.14 Write property test for pending payment polling picks up correct age range
    - **Property 14: Pending payment polling picks up correct age range**
    - **Validates: Requirements 12.3**
    - File: `backend/tests/property/test_payment_transitions.py`
  - [x] 13.15 Write property test for approval requires verified payment or force flag
    - **Property 17: Approval requires verified payment or force flag**
    - **Validates: Requirements 14.3, 4.6**
    - File: `backend/tests/property/test_submission_gates.py`

- [x] 14. Frontend property-based tests
  - [x] 14.1 Write property test for auto-save pauses during critical operations
    - **Property 15: Auto-save pauses during critical operations**
    - **Validates: Requirements 9.1, 9.2**
    - File: `apps/admissions/tests/property/autoSavePause.property.test.ts`
  - [x] 14.2 Write property test for submission payload excludes deprecated fields
    - **Property 16: Submission payload excludes deprecated fields**
    - **Validates: Requirements 2.5**
    - File: `apps/admissions/tests/property/submissionPayload.property.test.ts`

- [x] 15. Final checkpoint — All tests pass
  - Run `cd backend && python3 -m pytest` and `cd apps/admissions && bun run build` to verify everything compiles and passes.
  - Ensure all tests pass, ask the user if questions arise.

## Notes

- Tasks marked with `*` are optional and can be skipped for faster MVP
- Each task references specific requirements for traceability
- Checkpoints ensure incremental validation between phases
- Property tests validate universal correctness properties from the design document
- Backend uses Python (pytest + hypothesis), frontend uses TypeScript (Vitest + fast-check)
- No changes needed to WebhookProcessor, FeeResolver, or ProgramFeeViewSet — they are already correct
- **Neon project ID:** `wild-bar-37055823`
- **CTO Review findings (from live DB audit):**
  - `uq_program_fee_type_residency` full unique constraint must be dropped (task 1.2) — it conflicts with the partial index and blocks soft-delete replacement
  - Composite index `idx_payments_app_status` needed for double-payment prevention performance (task 1.3)
  - 0 program_fees records exist — seed data for 4 programs needed (task 1.4)
  - 25 existing applications have `payment_status = 'verified'` — backward compatibility required (task 4.4)
  - ~30+ frontend files still reference deprecated payment fields — full cleanup list in requirements
  - `useWizardController` still has full proof-of-payment upload logic (`popFile`, `handleProofOfPaymentUploadWrapped`) — must be removed (task 7.1)
