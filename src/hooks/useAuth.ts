/**
 * @deprecated Import auth state directly from `@/contexts/AuthContext`.
 *
 * This shim is kept temporarily for backwards compatibility while legacy
 * imports are migrated.
 */
export { useAuth } from '@/contexts/AuthContext'
